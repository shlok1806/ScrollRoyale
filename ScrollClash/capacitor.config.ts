import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aadit.scrollroyale',
  appName: 'ScrollRoyale',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
